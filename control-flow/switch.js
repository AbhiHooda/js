// switch (key) {
//     case value:
        
//         break;

//     default:
//         break;
// }

// for clubbing mutiple if else
const month = 3
switch (month) {
    case 1:
        console.log("Jan");
        
        break;

    case 3:
        console.log("Matched");
        break; // if bread not found once case is matched it will continue        

    default:
        console.log("Not found");
        
        break;
}